                                               Topics

Contictions
 1,If Else
 2,Switch

Function
  1,Function Declaration
  2,Function Expression
  3,Arrow Function

Task

1,Remove the duplicate number in an array
2,Collect the add and even number in an array

switch(variable/expression) {
    case value1:  
        // body of case 1
        break;

    case value2:  
        // body of case 2
        break;

    case valueN:
        // body of case N
        break;

    default:
        // body of default
}

// program using switch statement
let a = 2;

switch (a) {

    case 1:
        a = 'one';
        break;
    case 2:
        a = 'two';
        break;
    default:
        a = 'not found';
        break;
}
console.log(`The value is ${a}`);